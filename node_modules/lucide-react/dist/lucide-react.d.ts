import React, { useState, useEffect } from 'react';
import { motion } from 'framer-motion';
import { useAuth } from '../context/AuthContext';
import { LayoutDashboard as Layout, ShoppingBag, User, Heart, LogOut, Edit2 } from 'lucide-react';
import { cn } from '../lib/utils';
import { Link, useNavigate } from 'react-router-dom';

const Dashboard = () => {
  const { user, logout } = useAuth();
  const navigate = useNavigate();
  const [activeTab, setActiveTab] = useState('dashboard');

  // Redirect to login if no user data
  useEffect(() => {
    if (!user) {
      navigate('/login');
    }
  }, [user, navigate]);

  const menuItems = [
    { id: 'dashboard', label: 'Dashboard', icon: <Layout className="w-5 h-5" /> },
    { id: 'orders', label: 'Orders', icon: <ShoppingBag className="w-5 h-5" /> },
    { id: 'details', label: 'Account Details', icon: <User className="w-5 h-5" /> },
    { id: 'wishlist', label: 'Wishlist', icon: <Heart className="w-5 h-5" />, path: '/wishlist' },
  ];

  const userName = user?.email?.split('@')[0] || user?.username || 'Guest';

  const handleLogout = () => {
    logout();
    navigate('/login');
  };

  if (!user) {
    return null; // Will redirect via useEffect
  }

  return (
    <div className="min-h-screen bg-white pb-24">
      {/* Header */}
      <div className="py-24 text-center">
        <h1 className="text-5xl font-bold text-black tracking-tight">My Account</h1>
      </div>

      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-12">
          {/* Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-3xl border border-black/5 p-8">
              <div className="flex flex-col items-center mb-10">
                <div className="relative w-32 h-32 mb-4">
                  <div className="w-full h-full bg-black/10 rounded-full flex items-center justify-center overflow-hidden border-4 border-white">
                    <User className="w-16 h-16 text-black/20" />
                  </div>
                  <button className="absolute bottom-0 right-0 w-8 h-8 bg-black text-white rounded-full flex items-center justify-center border-2 border-white hover:scale-110 transition-transform">
                    <Edit2 className="w-3 h-3" />
                  </button>
                </div>
                <h3 className="text-xl font-bold text-black">{userName}</h3>
                {user?.full_name && (
                  <p className="text-sm text-black/60 mt-1">{user.full_name}</p>
                )}
              </div>

              <nav className="space-y-2">
                {menuItems.map((item) => (
                  item.path ? (
                    <Link
                      key={item.id}
                      to={item.path}
                      className={cn(
                        "w-full flex items-center space-x-4 px-6 py-4 rounded-2xl font-bold transition-all duration-300",
                        "text-black/60 hover:bg-black/5 hover:text-black"
                      )}
                    >
                      {item.icon}
                      <span>{item.label}</span>
                    </Link>
                  ) : (
                    <button
                      key={item.id}
                      onClick={() => setActiveTab(item.id)}
                      className={cn(
                        "w-full flex items-center space-x-4 px-6 py-4 rounded-2xl font-bold transition-all duration-300",
                        activeTab === item.id 
                          ? "bg-black text-white shadow-lg shadow-black/10" 
                          : "text-black/60 hover:bg-black/5 hover:text-black"
                      )}
                    >
                      {item.icon}
                      <span>{item.label}</span>
                    </button>
                  )
                ))}
                <button
                  onClick={handleLogout}
                  className="w-full flex items-center space-x-4 px-6 py-4 rounded-2xl font-bold text-black/60 hover:bg-red-50 hover:text-red-500 transition-all duration-300"
                >
                  <LogOut className="w-5 h-5" />
                  <span>Logout</span>
                </button>
              </nav>
            </div>
          </div>

          {/* Main Content */}
          <div className="lg:col-span-3">
            <motion.div
              key={activeTab}
              initial={{ opacity: 0, x: 20 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.3 }}
              className="space-y-8"
            >
              {activeTab === 'dashboard' && (
                <div className="space-y-6">
                  <h2 className="text-4xl font-bold text-black">Hello {userName},</h2>
                  <p className="text-lg text-black/60 leading-relaxed max-w-2xl">
                    From your account dashboard you can view your <Link to="/orders" className="text-black font-medium hover:underline">recent orders</Link>, manage your <Link to="/profile" className="text-black font-medium hover:underline">account details</Link>, and check your <Link to="/wishlist" className="text-black font-medium hover:underline">wishlist</Link>.
                  </p>
                  
                  {/* Quick Stats */}
                  <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mt-8">
                    <div className="bg-black/5 rounded-2xl p-6 text-center">
                      <ShoppingBag className="w-8 h-8 text-black/40 mx-auto mb-3" />
                      <h3 className="text-2xl font-bold text-black">0</h3>
                      <p className="text-sm text-black/60">Total Orders</p>
                    </div>
                    <div className="bg-black/5 rounded-2xl p-6 text-center">
                      <Heart className="w-8 h-8 text-black/40 mx-auto mb-3" />
                      <h3 className="text-2xl font-bold text-black">0</h3>
                      <p className="text-sm text-black/60">Wishlist Items</p>
                    </div>
                    <div className="bg-black/5 rounded-2xl p-6 text-center">
                      <User className="w-8 h-8 text-black/40 mx-auto mb-3" />
                      <h3 className="text-2xl font-bold text-black">Member</h3>
                      <p className="text-sm text-black/60">Since 2024</p>
                    </div>
                  </div>
                </div>
              )}

              {activeTab === 'orders' && (
                <div className="bg-black/5 rounded-[40px] p-12 text-center">
                  <ShoppingBag className="w-16 h-16 text-black/10 mx-auto mb-6" />
                  <h3 className="text-2xl font-bold mb-2">No orders yet</h3>
                  <p className="text-black/40 mb-8">You haven't placed any orders yet. Explore our collection to find something you love.</p>
                  <Link to="/products" className="inline-flex px-8 py-4 bg-black text-white rounded-2xl font-bold hover:bg-black/80 transition">
                    Start Shopping
                  </Link>
                </div>
              )}

              {activeTab === 'details' && (
                <div className="bg-white rounded-3xl border border-black/5 p-12">
                  <h3 className="text-2xl font-bold mb-8">Account Details</h3>
                  <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <div className="space-y-2">
                      <label className="text-[10px] font-bold text-black/40 uppercase tracking-widest ml-1">Email Address</label>
                      <input 
                        disabled 
                        value={user?.email || ''} 
                        className="w-full px-4 py-3 bg-black/5 rounded-xl text-sm font-medium"
                      />
                    </div>
                    <div className="space-y-2">
                      <label className="text-[10px] font-bold text-black/40 uppercase tracking-widest ml-1">Username</label>
                      <input 
                        disabled 
                        value={user?.username || ''} 
                        className="w-full px-4 py-3 bg-black/5 rounded-xl text-sm font-medium"
                      />
                    </div>
                    <div className="space-y-2 md:col-span-2">
                      <label className="text-[10px] font-bold text-black/40 uppercase tracking-widest ml-1">Full Name</label>
                      <input 
                        disabled 
                        value={user?.full_name || 'Not provided'} 
                        className="w-full px-4 py-3 bg-black/5 rounded-xl text-sm font-medium"
                      />
                    </div>
                  </div>
                </div>
              )}
            </motion.div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Dashboard;